{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Panier{% endblock %}

{% block body %}
           {# ****************** PANIER ****************** #}
	<section class="cart">
    <div class="cart__container">
        
        <div class="cart__wrapper">
                <h2 class="cart__title">Mon panier</h2>
                <a class="cart__clear" href="#">x Vider le panier</a>
                <div class="cart__item-wrapper">
                    {% include "./_partials/_cart/-cart-item.html.twig" %}
                    <div class="cart__total">
                        <p class="cart__total-amount">Total panier: <span class="cart__total-price">8,50€</span></p>
                        <p class="cart__total-info">Frais de livraison: Offert </p>
                    </div>
                </div>
                <a href="#" class="btn-secondary">
                    <span class="link">Valider la commande</span>
                </a>
        </div>

    </div>
</section>
{% endblock %}
